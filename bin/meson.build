install_data(
    meson.project_name(),
    install_dir: join_paths(get_option('bindir'))
)

install_data(
    'io.elementary.dpms-greeter-helper',
    install_dir: join_paths(get_option('bindir'))
)
